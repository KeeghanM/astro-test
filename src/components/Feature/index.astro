---
    interface Props {
        icon: string;
        title: string;
        description: string;
    }

const { icon, title, description } = Astro.props;
---

<div>
    <img src={icon} />
    <h3>{title}</h3>
    <p>{description}</p>
</div>
